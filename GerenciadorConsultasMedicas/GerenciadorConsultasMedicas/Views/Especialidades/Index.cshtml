@model IEnumerable<GerenciadorConsultasMedicas.Models.Especialidades>

@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EspecialidadeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EspecialidadeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EspecialidadeID })
        </td>
    </tr>
}

</table>
